# ==================================
# THE GREAT BEANS - Backend Environment Variables
# ==================================
# PostgreSQL is REQUIRED for production (roadmap.txt)
# Use Docker Compose for local development: docker-compose up -d
#
# IMPORTANT: Copy this file to .env before starting Strapi
# cp env.example .env

# ----------------------------------
# Server Configuration
# ----------------------------------
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# ----------------------------------
# Secrets (REQUIRED - Generate these!)
# ----------------------------------
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"
# Run the above command 4 times for APP_KEYS (comma-separated)
APP_KEYS=generate_with_openssl_1,generate_with_openssl_2,generate_with_openssl_3,generate_with_openssl_4
API_TOKEN_SALT=generate_with_openssl
ADMIN_JWT_SECRET=generate_with_openssl
TRANSFER_TOKEN_SALT=generate_with_openssl
JWT_SECRET=generate_with_openssl

# ----------------------------------
# Database - PostgreSQL (REQUIRED)
# ----------------------------------
# Default: Docker Compose PostgreSQL (docker-compose up -d)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=thegreatbeans
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_dev_password

# Production Database (Supabase/Managed PostgreSQL)
# Uncomment and configure for production:
# DATABASE_HOST=db.xxxxxxxxxxxxx.supabase.co
# DATABASE_PORT=5432
# DATABASE_NAME=postgres
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=your_secure_production_password
# DATABASE_SSL=true
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Connection Pool Configuration
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10
DATABASE_ACQUIRE_TIMEOUT=60000

# ----------------------------------
# CORS Configuration
# ----------------------------------
# Development: Allow localhost:3000
CORS_ORIGIN=http://localhost:3000

# Production: Use your frontend domain
# CORS_ORIGIN=https://thegreatbeans.com,https://www.thegreatbeans.com

# ----------------------------------
# Admin Panel
# ----------------------------------
# Optional: Custom admin path for security
# ADMIN_PATH=/admin

# ----------------------------------
# Upload Provider (Production)
# ----------------------------------
# Use Cloudinary or AWS S3 for production
# CLOUDINARY_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# AWS S3 Configuration
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_ACCESS_SECRET=your_secret_key
# AWS_REGION=us-east-1
# AWS_BUCKET=thegreatbeans-uploads

# ----------------------------------
# Email Provider (Optional)
# ----------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# ----------------------------------
# Security
# ----------------------------------
# Rate limiting (requests per minute)
# RATE_LIMIT_MAX=100

# ----------------------------------
# NOT RECOMMENDED: SQLite (testing only)
# ----------------------------------
# Only use for quick local testing
# DATABASE_CLIENT=sqlite
# DATABASE_FILENAME=.tmp/data.db




